

CREATE KEYSPACE test_gs with replication = { 'class' : 'SimpleStrategy', 'replication_factor' : 1 };


create table notespace.notes(
    noteid      uuid,
    userid      uuid,
    content     text,
    PRIMARY KEY (noteid));



INSERT INTO test_gs.note(noteid, userid, content, created) values ( uuid(), uuid(), 'some content1', toTimestamp(now()));
INSERT INTO test_gs.note(noteid, userid, content, created) values ( uuid(), uuid(), 'some content2', toTimestamp(now()));
INSERT INTO test_gs.note(noteid, userid, content, created) values ( uuid(), uuid(), 'some content3', toTimestamp(now()));


CREATE INDEX note_by_user ON test_gs.note ( userid );


